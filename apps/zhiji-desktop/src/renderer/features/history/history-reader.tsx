import type { HistoryItem } from '../../domain/history-items';
const labels = { journal: '日志', daily: '日反馈', weekly: '周报', monthly: '月报', project: '项目复盘' };
export function HistoryReader({ item, onEdit, onGenerateDaily }: { item: HistoryItem; onEdit?(id: string): void; onGenerateDaily?(date: string): void }) { return <article className="card history-reader"><div className="reader-meta"><span className="tag">{labels[item.kind]}</span><time>{item.date}</time>{item.kind === 'journal' && onGenerateDaily && <button onClick={() => onGenerateDaily(item.date)}>生成这一天的反馈</button>}{item.kind === 'journal' && onEdit && <button onClick={() => onEdit(item.id)}>编辑</button>}</div><h2>{item.title}</h2><pre>{item.body}</pre>{item.sourceIds.length > 0 && <footer><strong>材料来源</strong><p>{item.sourceIds.join('、')}</p></footer>}</article>; }
