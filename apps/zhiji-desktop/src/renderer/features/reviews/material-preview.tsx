export function MaterialPreview({ sources }: { sources: { id: string; date: string; excerpt: string }[] }) { return <section className="material-preview"><div className="section-heading"><h3>本次材料</h3><span className="tag">{sources.length} 条</span></div><div className="material-list">{sources.map((source) => <article key={source.id}><time>{source.date}</time><div><p>{source.excerpt}</p><small>{source.id}</small></div></article>)}</div></section>; }
