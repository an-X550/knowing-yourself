{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'STATUS=$(git status --porcelain 2>/dev/null); if [ -n \"$STATUS\" ]; then echo; echo \"⚠️ 提醒：仍有未提交改动。自动提交可能未完成，请运行 /提交 本地提交；提交后运行 git push 推送。\"; echo; fi'"
          }
        ]
      }
    ]
  }
}
