# SETUP.md — AI 自执行项目初始化

> 将此文件放到任意项目根目录，告诉 AI "读取并执行 SETUP.md"。
> AI 将自动规范化目录结构并创建 CLAUDE.md、PROJECT_STATUS.md、
> CHANGELOG.md、VERSION —— 零依赖，空目录即可运行。

---

## 执行阶段

按以下三个阶段顺序执行，每阶段完成后自我验证。

### 阶段A：目录规范化

1. 列出所有文件（`find . -type f`），按以下标准标记归属：

| 文件类型 | 目录 | 版本控制 |
|---------|------|---------|
| 项目文档、方法论、模板 | `docs/` | 纳入 |
| 脱敏示例 | `examples/` | 纳入 |
| 测试代码 | `tests/` | 纳入 |
| Issue/PR 模板、CI | `.github/` | 纳入 |
| 个人数据、原始输入 | `data/` | **gitignore** |
| 生成的分析输出 | `output/` | **gitignore** |
| 个人画像、快照 | `关于我/` | **gitignore** |

2. 创建缺失目录。移动/重命名文件：纳入版本控制的目录统一用英文 kebab-case。
3. 创建缺失的标准文件：`LICENSE`（MIT）、`.github/ISSUE_TEMPLATE/bug_report.md`、`.github/PULL_REQUEST_TEMPLATE.md`。
4. 扩展 `.gitignore`：排除 `data/` `output/` `关于我/`；保留 `!examples/`。
5. 用 grep 定位旧路径引用，批量替换，删除空目录，验证无残留。

不可移动的目录：`.git/`、`.github/`、平台固定目录（`.claude/`）、构建输出（`dist/` `build/` `node_modules/`）。

### 阶段B：生成文件

#### B1. PROJECT_STATUS.md

扫描代码库，生成 `PROJECT_STATUS.md`，包含以下章节：

1. **项目概述** — 一句话描述 + 目标用户/场景
2. **技术栈** — 表格：平台、语言、框架、数据库、版本控制
3. **架构设计** — 分层说明 + 核心模块列表及职责
4. **当前进度** — 各模块状态（已完成/进行中/未开始）
5. **待办事项** — 高/中/低优先级
6. **已知问题** — 分类：性能、安全、兼容性、未完成功能
7. **关键决策记录** — 日期 + 决策 + 理由

格式：YAML frontmatter（`created`, `last_updated`）。标题下紧跟 `**当前版本**：0.1.0`。

#### B2. CHANGELOG.md

```markdown
---
created: [当前日期]
last_updated: [当前日期]
---

# CHANGELOG — 改动记录

> 倒序时间线。每次代码修改后自动追加。

## [当前日期] 已完成 项目初始化

- **受影响文件**: `PROJECT_STATUS.md`, `CHANGELOG.md`, `CLAUDE.md`, `VERSION` (新增)
- **改动摘要**: 项目状态管理基础设施初始化
```

#### B3. CLAUDE.md

直接写入以下内容（不要写"复制自XX项目"）：

```markdown
# CLAUDE.md — [项目名]

## 元规则

**冲突解决**：当多条规则冲突时，具体规则优先于通用规则。

**跳过阈值**：以下情况可跳过非必要规则，直接执行：
- 纯信息查询、阅读文件回答问题
- 单文件小改动（<20 行变更）
- 用户明确说"直接改"/"跳过"/"不用走流程"

---

## 一、上下文加载

每次对话开始时，按顺序完整阅读：
1. `PROJECT_STATUS.md`
2. `CHANGELOG.md`

若文件不存在，根据代码库主动扫描创建。启动后校验 `VERSION` 与 PROJECT_STATUS 版本号是否一致，不一致时以 VERSION 为准修正。

**PROJECT_STATUS 章节**：项目概述、技术栈、架构设计、当前进度、待办事项、已知问题、关键决策记录。YAML frontmatter。标题下 `**当前版本**：X.Y.Z`。

**CHANGELOG 格式**：倒序时间线。每条记录：
```
## [YYYY-MM-DD HH:MM] [标签] 改动简述 (v旧 → v新)
- **受影响文件**: 文件列表
- **改动摘要**: 简述
```
标签：`[功能]` `[修复]` `[重构]` `[文档]` `[配置]` `[破坏性变更]` `[回退]`

---

## 二、改动追踪

**自动记录（主机制）**：每次修改源代码文件后，响应结束前自动在 CHANGELOG 顶部追加记录。同任务合并。未改代码不记录。

**手动跟踪（辅机制）**：大型多步骤任务可先加 `## [时间] [简述] — 待开始`，完成后更新为 `已完成`。

**同步 PROJECT_STATUS**：改动影响 PROJECT_STATUS 信息时，同步更新并将 `last_updated` 设为当前日期。

**文档自动同步**：改动完成后自动检查——路径变更→grep修复过期引用；结构变更→更新README目录结构图；功能变更→补充README命令表。无需用户提醒。

---

## 三、版本管理

语义化版本：`主版本.次版本.修订号`，存于 `VERSION` 文件（纯文本）。

修改代码并更新 CHANGELOG 后自动递增：
- 修复/文档/小优化 → 修订号 +1（1.2.3 → 1.2.4）
- 新功能（向后兼容）→ 次版本号 +1，修订号归零（1.2.3 → 1.3.0）
- 破坏性变更 → 主版本号 +1，次版本和修订号归零（1.2.3 → 2.0.0）

同步：CHANGELOG 标题标注 `(v旧→v新)`，同步 PROJECT_STATUS 版本字段。

---

## 四、代码风格

- Markdown 文件使用 YAML frontmatter
- 文件路径相对项目根目录；文件名英文 kebab-case；gitignored 目录可用中文
- 表格优先用于结构化信息

---

## 五、目录管理

根目录仅保留核心文件（≤8个）。其他文件按类型归入子目录：

| 目录 | 用途 | 版本控制 |
|------|------|---------|
| 源码目录 | 源代码 | 纳入 |
| `docs/` | 文档、模板、spec | 纳入 |
| `examples/` | 脱敏示例 | 纳入 |
| `tests/` | 测试代码 | 纳入 |
| `.github/` | Issue/PR/CI | 纳入 |
| `data/` | 个人数据 | **gitignore** |
| `output/` | 生成输出 | **gitignore** |
| `关于我/` | 个人画像 | **gitignore** |

新增文件时检查归属。目录不合规时：标记归属 → 创建目录 → 移动文件 → 更新引用 → 更新 gitignore → 删除空目录 → 验证。

---

## 六、工作流控制

### 回退与撤销

触发词："回退上次修改"/"回退到 vX.Y.Z"/"撤销对X的改动"。
流程：读 CHANGELOG 定位 → 选回退方法 → 执行 → 版本号回退 → 追加 `[回退]` 记录。
方法：git 跟踪未 commit → `git checkout HEAD -- <file>`；已 commit → `git revert`；未跟踪 → 手动反向编辑。
回退前必须确认影响范围。回退后验证内容、版本号、CHANGELOG 完整。

### Spec-Before-Code

收到新功能需求时不得直接编码。在 `docs/specs/` 创建 `<功能名>.md`（需求目标+边界约束+验收标准+实施计划+状态）。高优先级需求在 PROJECT_STATUS.md 留链接。展示摘要等待确认。确认后状态→"开发中"→编码→完成后→"已完成"并勾选 PROJECT_STATUS。spec 文件本身不触发改动追踪。需求模糊时先澄清再写 spec。
```

根据目标项目调整 `[项目名]` 和代码风格细节，但6组规则的核心逻辑必须保留。

#### B4. VERSION

```bash
echo -n "0.1.0" > VERSION
```

#### B5. Spec 模板

创建 `docs/specs/_TEMPLATE.md`：
```markdown
---
created: [日期]
status: 规划中
---

# 需求目标

[一句话描述]

# 边界约束

- 不做什么：[明确排除]
- 影响范围：[受影响模块/文件]
- 技术约束：[平台/兼容性限制]

# 验收标准

- [ ] [可验证的条件]

# 实施计划

1. [步骤1：创建/修改 文件A — 做什么]
2. [步骤2：创建/修改 文件B — 做什么]

# 当前状态：规划中
```

### 阶段C：验证与报告

1. 确认所有文件存在：`PROJECT_STATUS.md` `CHANGELOG.md` `CLAUDE.md` `VERSION` `LICENSE`
2. 确认 `.gitignore` 包含 `data/` `output/` `关于我/`
3. 确认目录结构符合阶段A标准
4. 向用户报告：创建的文件列表、规则数量、确认规则已生效

---

## 执行约束

- 每阶段结束用 `ls`/`grep` 自验证
- 已有文件→合并，不覆盖；冲突→保留已有+追加
- 不确定→标注"待确认"或询问
- 已有 CLAUDE.md→保留已有规则，追加新规则并重新编号
- 已有 VERSION→不覆盖，按现有版本继续递增
