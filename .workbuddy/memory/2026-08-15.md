# 2026-08-15

## 桌面端重新打包排障（00:25 完成）

- 用户发现 out/知己-win32-x64 的 exe 仍是 8/14 20:01：v1.26.0 只提交源码未重新打包。exe 是构建产物，必须 `npm run package` 才更新。
- 打包两个环境坑（本机 WorkBuddy 环境长期有效）：
  1. NODE_OPTIONS 注入的 genie-safe-delete.cjs 会把构建工具的 rm 拦到回收站并失败（"Some operations were aborted"）→ 打包时覆盖 `NODE_OPTIONS="--use-system-ca"`。
  2. HTTP_PROXY/HTTPS_PROXY 指向 127.0.0.1:7897（Clash 没开）→ electron-forge 下载/校验 ECONNREFUSED → 打包前置空 `HTTP_PROXY= HTTPS_PROXY=`。
- 可用命令：`HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= NODE_OPTIONS="--use-system-ca" npm run package`
- 结果：新 exe 225MB @ 2026-08-15 00:25，含 v1.26.0 全部改动。
